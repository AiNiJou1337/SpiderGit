# 数据库连接URL
DATABASE_URL="postgresql://postgres:password@localhost:5432/github_trending"

# 应用环境
NODE_ENV="development"

# 应用端口
PORT=3000

# Python 解释器（可选），默认按以下顺序解析并校验版本 >= 3.12：
# 1) PYTHON_BIN 指定；2) Windows: py -3.12 / python3.12 / python；Linux/Mac: python3.12 / python3 / python
# 未满足时会报错提示
PYTHON_BIN=

# GitHub API Token（动态配置，支持任意数量）
# 命名格式：GITHUB_TOKEN_<任意标识符>
# 系统会自动发现并验证所有以 GITHUB_TOKEN_ 开头的环境变量

# 主要 Token
GITHUB_TOKEN_MAIN=ghp_your_main_token_here

# 备用 Token（可选）
GITHUB_TOKEN_BACKUP1=
GITHUB_TOKEN_BACKUP2=

# 你可以添加更多 Token，只要以 GITHUB_TOKEN_ 开头即可
# GITHUB_TOKEN_EXTRA=